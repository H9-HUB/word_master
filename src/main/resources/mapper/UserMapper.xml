<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.word_master.mapper.UserMapper">
    <select id="selectByUsername" parameterType="String" resultType="com.example.word_master.entity.User">
        SELECT id, username, password, create_time, last_login
        FROM user
        WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.example.word_master.entity.User">
        INSERT INTO user (username, password, create_time)
        VALUES (#{username}, #{password}, NOW())
    </insert>

    <update id="updateLastLogin" parameterType="String">
        UPDATE user
        SET last_login = NOW()
        WHERE username = #{username}
    </update>

    <select id="selectById" parameterType="Integer" resultType="com.example.word_master.entity.User">
        SELECT id, username, password, create_time, last_login
        FROM user
        WHERE id = #{id}
    </select>
    <update id="updatePassword">
        UPDATE user
        SET password = #{newPassword}  WHERE id = #{userId} AND password = #{oldPassword}
    </update>

    <update id="updateUsername">
        UPDATE user
        SET username = #{username}     WHERE id = #{id}
    </update>

</mapper>
